let log = [];
if (fs.existsSync(STATUS_FILE)) {
  try {
    const content = fs.readFileSync(STATUS_FILE, 'utf8');
    const parsed = JSON.parse(content);
    log = Array.isArray(parsed) ? parsed : [];
  } catch (err) {
    console.error("Invalid JSON in status.json, starting fresh.");
    log = [];
  }
}
